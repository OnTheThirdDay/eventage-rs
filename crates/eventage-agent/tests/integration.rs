// Integration tests have moved to eventage-provided-impl/tests/integration.rs
// to avoid circular dependencies (eventage-provided-impl depends on eventage-agent,
// so tests that cover the full stack must live in the higher-level crate).
